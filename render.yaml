services:
  - type: web
    name: alien-llm-chat
    env: python
    buildCommand: "./build_cpp.sh && pip install -r requirements.txt"
    startCommand: "gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
